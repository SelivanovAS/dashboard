[pytest]
; Оба каталога тестов одним прогоном: `python3 -m pytest` из корня.
; scripts/__init__.py даёт scripts/tests уникальное пакетное имя —
; без него коллизия с корневым пакетом tests (ModuleNotFoundError).
testpaths = tests scripts/tests
